# Example environment variables
# Copy this file to .env.development for local development

# Tenant Configuration
VUE_APP_TENANT_NAME=acme
VUE_APP_TITLE=Acme Corporation Portal

# API Configuration
VUE_APP_AUTH_API_URL=http://localhost:5130
VUE_APP_API_BASE_URL=http://localhost:5142
VUE_APP_TENANT_API_URL=http://localhost:5121

# Build Configuration (used during Docker build)
VUE_APP_ENVIRONMENT=development

# Note: LogoUrl and ThemeColor are now fetched dynamically from the Tenant API
# In production, tenant name is automatically extracted from the URL subdomain
# Example: acme.demodeck.xyz will automatically set tenant to 'acme'